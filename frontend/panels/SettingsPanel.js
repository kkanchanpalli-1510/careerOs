// frontend/panels/SettingsPanel.js
// Placeholder — replaced by a future doc

export function render(container, session) {
  container.innerHTML = `
    <div style="padding:40px; color:var(--t3);
                font-family:'DM Mono',monospace; font-size:11px;">
      settings panel — coming in next build session
    </div>
  `;
}
