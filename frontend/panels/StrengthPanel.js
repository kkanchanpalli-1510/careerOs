// frontend/panels/StrengthPanel.js
// Placeholder — replaced by doc 12b

export function render(container, session) {
  container.innerHTML = `
    <div style="padding:40px; color:var(--t3);
                font-family:'DM Mono',monospace; font-size:11px;">
      strength panel — coming in next build session
    </div>
  `;
}
